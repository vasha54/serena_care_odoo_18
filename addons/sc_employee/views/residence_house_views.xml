<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- View residence_house form -->
    <record id="view_residence_house_form_employee" model="ir.ui.view">
        <field name="name">view.residence_house.form</field>
        <field name="model">residence_house</field>
        <field name="inherit_id" ref="sc_residence.view_residence_house_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button 
                    name="open_reassignment_employee_wizard" 
                    string="Reasignar empleados" 
                    type="object"
                    class="btn-primary"
                    invisible="is_deleted or not employee_ids"/>
            </xpath>  
            <xpath expr="//page[@name='services']" position="after">
                <page name="employees" string="Empleados">
                    <field name="employee_ids" options="{'create':false, 'edit':false, 'delete':false}">
                        <list default_order="name asc">
                            <field string="Nombre" name="name"/>
                            <field string="Departamento"  name="department_id" />
                            <field string="Ocupación" name="job_id" />
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>


    <!-- View residence_house list -->
    <record id="view_residence_house_list_employee" model="ir.ui.view">
        <field name="name">view.residence_house.list</field>
        <field name="model">residence_house</field>
        <field name="inherit_id" ref="sc_residence.view_residence_house_list"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field string="Cant. Empleados" name="employee_count" />
            </xpath>
        </field>
    </record>

</odoo>