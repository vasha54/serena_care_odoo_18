<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View scalefrail.state View List -->
    <record id="view_scalefrail_state_list" model="ir.ui.view">
        <field name="name">Vista Lista Estados de Fragilidad</field>
        <field name="model">scalefrail.state</field>
        <field name="arch" type="xml">
            <list>
                <field name="active" string="Activo" widget="boolean_toggle"/>
                <field name="name"/>
                <field name="min_score" width="180"/>
                <field name="max_score" width="180"/>
            </list>
        </field>
    </record>

    <!-- View scalefrail.state form -->
    <record id="view_scalefrail_state_form" model="ir.ui.view">
        <field name="name">Vista Formulario Estado de Fragilidad</field>
        <field name="model">scalefrail.state</field>
        <field name="arch" type="xml">
            <form string="Scalefrail State">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Nombre..."/>
                        </h1>
                        <group>
                            <group>
                                <field string="Activo" name="active" widget="boolean_toggle"/>
                            </group>
                            <group>
                                <field name="min_score" />
                                <field name="max_score" />
                            </group>
                        </group>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- View scalefrail.state search -->
    <record id="view_scalefrail_state_search" model="ir.ui.view">
        <field name="name">Vista Búsqueda Estado de Fragilidad</field>
        <field name="model">scalefrail.state</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" />
            </search>
        </field>
    </record>

    <!-- Action scalefrail.state -->
    <record id="action_scalefrail_state" model="ir.actions.act_window">
        <field name="name">Estado de fragilidad</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">scalefrail.state</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                No hay registros, haga clic aquí para agregar un nuevo estado de fragilidad.
            </p>
        </field>
    </record>

</odoo>
