<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View medicament.dosage View List -->
    <record id="view_medicament_dosage_list" model="ir.ui.view">
        <field name="name">Vista Lista Dosis de Medicamento</field>
        <field name="model">medicament.dosage</field>
        <field name="arch" type="xml">
            <list>
                
            </list>
        </field>
    </record>

    <!-- View medicament.dosage form -->
    <record id="view_medicament_dosage_form" model="ir.ui.view">
        <field name="name">Vista Formulario Dosis de Medicamento</field>
        <field name="model">medicament.dosage</field>
        <field name="arch" type="xml">
            <form string="Medicament Dosage">
                <sheet>
                    <div class="oe_title">
                        <!-- <label for="name"/> -->
                        <h1>
                            
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- View medicament.dosage search -->
    <record id="view_medicament_dosage_search" model="ir.ui.view">
        <field name="name">Vista Búsqueda Dosis de Medicamento</field>
        <field name="model">medicament.dosage</field>
        <field name="arch" type="xml">
            <search>
               
            </search>
        </field>
    </record>

    <!-- Action medicament.dosage -->
    <record id="action_medicament_dosage" model="ir.actions.act_window">
        <field name="name">Medicament Dosage</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">medicament.dosage</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Medicament Dosage.
            </p>
        </field>
    </record>

</odoo>
